# Session — August 3, 2026

## What We Did

### Step 0 — Add Your Product
- Added **"My product is under 4 lbs"** checkbox to the checkbox group
- Added to the `canContinue` disabled condition and skip reset

### Step 2 — Shipping
- Added **purple note**: "Please note, a COMPLETED Recipient List, including all sizes, is required to start production on your order."
- Iterated on order several times per Cody's feedback — final order:
  1. Intro description: "Upload your recipient list or we can send it direct to you."
  2. Ship to my list (radio)
  3. Download shipping list template button
  4. Upload your list dropzone
  5. Purple note
  6. Ship to me (radio)

### Step 5 — Message
- Added note: "All messages are printed on an external note card…" (Cody's copy)
- Added **"I confirm this message"** radio row
- Both moved into the fixed **bottom-area** so they're always visible above the nav buttons
- Fixed textarea height: changed from fixed 338px → `flex: 1` → `overflow: hidden` → final fix: `min-height: 56%` (user's DevTools find)

### Step 4 — Add Some Booze
- Moved **TBB Compliant Shipping** note into the `bottom-area`

### $1 Send-In Fee (WooCommerce)
- Created `wc-add-sendin-fee.php` — PHP snippet that catches `?cm_addfee={qty}` on the checkout page, removes any existing send-in line items, and adds `SENDIN_PRODUCT_ID` with kit quantity
- Checkout URL in App.jsx updated: if `addProduct`, appends `?cm_addfee={qty}` to the WC checkout URL
- **Cody to do**: create a WC product at $1 (virtual/simple), set `SENDIN_PRODUCT_ID` in the snippet, add to Code Snippets

### WP Embed Plan
- Created `WP-EMBED-PLAN.md` — full migration plan for moving the React app from Vercel into WordPress

### Fixes
- `wc-product-configurator.php`: wrapped file input detection in `if (isMob)` — "Tap to upload logo" button was incorrectly appearing on desktop product pages
- `wc-redirect-after-tee.php`: reverted to pre-session working state after it broke

## Pending
- Copy updated `wc-redirect-after-goods.php` into WP Code Snippets (URL params approach for cross-origin image/detail capture)
- Copy updated `wc-product-configurator.php` into WP Code Snippets (desktop logo fix)
- Cody: create $1 WC product + set `SENDIN_PRODUCT_ID` + add snippet
- Verify end-to-end goods flow for Cap, Tote, Tumbler, Journal
- Replace policy link `href="#"` with Cody's actual URL
